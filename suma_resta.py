import modulo_santiago

#llamamos módulo de Santiago
a = validacion()
def suma(a):
    monto = 0
    if a == True:
        return monto += 100
    else:
        return monto -= 100
    print('Su monto es', monto)